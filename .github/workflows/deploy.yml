name: Build and Deploy

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v2.3.1
        with:
          persist-credentials: false

      - name: Build and Deploy
        uses: renzhaosy/hexo-deploy-action@master
        env:
          PERSONAL_TOKEN: ${{ secrets.DEPLOY_TOKEN }}
          PUBLISH_REPOSITORY: oxyzhg/oxyzhg.github.io # The repository the action should deploy to.
          BRANCH: gh-pages  # The branch the action should deploy to.
          PUBLISH_DIR: ./public # The folder the action should deploy.

      # # Caching dependencies to speed up workflows. (GitHub will remove any cache entries that have not been accessed in over 7 days.)
      # - name: Cache node modules
      #   uses: actions/cache@v1
      #   id: cache
      #   with:
      #     path: node_modules
      #     key: ${{ runner.os }}-node-${{ hashFiles('**/package-lock.json') }}
      #     restore-keys: |
      #       ${{ runner.os }}-node-
      # - name: Install Dependencies
      #   if: steps.cache.outputs.cache-hit != 'true'
      #   run: npm ci
      
      # # Deploy hexo blog website.
      # - name: Deploy
      #   id: deploy
      #   uses: sma11black/hexo-action@v1.0.3
      #   with:
      #     deploy_key: ${{ secrets.DEPLOY_TOKEN }}
      #     user_name: Kq  # (or delete this input setting to use bot account)
      #     user_email: oxyzhg@163.com  # (or delete this input setting to use bot account)
      #     commit_msg: ${{ github.event.head_commit.message }}  # (or delete this input setting to use hexo default settings)

      # # Use the output from the `deploy` step(use for test action)
      # - name: Get the output
      #   run: |
      #     echo "${{ steps.deploy.outputs.notify }}"

      # - name: Install and Build
      #   run: |
      #     npm install
      #     npm run build

      # - name: Deploy
      #   uses: JamesIves/github-pages-deploy-action@3.7.1
      #   with:
      #     ACCESS_TOKEN: ${{ secrets.DEPLOY_TOKEN }}
      #     BRANCH: gh-pages
      #     FOLDER: build
      #     REPOSITORY_NAME: oxyzhg/oxyzhg.github.io
